import 'package:flutter/material.dart';

class AppointmentActivity extends StatelessWidget {
  const AppointmentActivity({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // ignore: sized_box_for_whitespace
    return const Center(
      child: Text(
        'Appointments',
        style: TextStyle(fontSize: 50.0),
      ),
    );
  }
}
